# LIVE FAILURE CARD

**If `demo.ps1` hangs or fails:**
Run the manual Python commands directly from `DEMO_COMMAND_CARD.md`.

**If the scanner accidentally hits your real `~/.claude.json` and screams COMPROMISED:**
Say: "This perfectly demonstrates Layer 0 global enumeration! It found my personal system config and flagged standard OAuth tokens because they structurally resemble base64 payloads (S7). Let's return to the isolated demo fixtures." Then run the specific file commands.

**If the mentor asks to scan a random, novel file and it throws a False Positive:**
Say: "This is expected behavior for a deterministic Layer 1 engine. It structurally matches our regex. This perfectly demonstrates why we designed the future Layer 3 LLM—to evaluate ambiguous semantic intent."

**If the benchmark numbers don't match the slides:**
Open `mentor/REHEARSAL_REPORT.md` and state: "The repository was frozen at 12/13 True Positives and 0 False Positives. Any live drift means the corpus files were modified."
