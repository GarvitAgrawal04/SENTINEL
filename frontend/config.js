// Where is the Sentinel API?
//   ""  (default) = find it automatically: the server that served this page, then http://127.0.0.1:8000
//   "https://your-api.example.com" = always use this one (set it when you host this folder as a static site)
window.SENTINEL_CONFIG = { apiBase: "" };
